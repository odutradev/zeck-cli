{
  "name": "zeck-cli",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "tsc && tsc-alias",
    "dev": "tsx watch lib/cli.ts",
    "codemerge": "node dist/cli.js",
    "publish": "nxp pub --bump-package --build --build-zip --clean --push --npm-publish",
    "prepublishOnly": "npm run build"
  }
}
